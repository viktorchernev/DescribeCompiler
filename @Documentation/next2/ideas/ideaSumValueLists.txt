IDEA:sum value lists
value at the title of the list is automatically added, and is a sum of values in each entry. For example:

daily expenses (31) ->

	launch (22),
	coffee (5),
	water (1),
	bus (3);

in this example, (31) is filled in automatically.

This is to be achieved with decorators.