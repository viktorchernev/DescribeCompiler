FEATURE: Verbosity - None

A verbosity level where there is no output but a single number at the end of the parsing process, indicating how many files were parsed if successful, or -1 if unsuccessful. This is useful for shell execute from another exe, where we just want a single number as a return.